
public class constructorMethod_theory {
/*
 * Constructor Method: it is a default method of the class that will be run at the time when the 
 * object is created or initiated
 * 
 * constructor method is used to declare default behaviour of all objects
 * 
 * constructor name must be same as its class name
 * 
 * construct method must have no explicit return type
 * 
 * There are two types of constructors:
 * 	   Default constructor (no-args constructor)
 * 	   Parameterized constructor.
 */
}
